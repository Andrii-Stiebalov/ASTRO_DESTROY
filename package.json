{
  "name": "my-pixi-game",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist",
    "build:gh-pages": "vite build --base=/my-pixi-game/"
  },
  "devDependencies": {
    "typescript": "~5.8.3",
    "vite": "^7.1.6"
  },
  "dependencies": {
    "gh-pages": "^6.3.0"
  }
}
